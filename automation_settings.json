{
    // Automation-specific settings for AutomatedTrader
    "SCAN_INTERVAL": 3600,        // Signal scan interval in seconds (300-7200)
    "TOP_N_SIGNALS": 5,           // Max signals per cycle (1-20)
    "MAX_DAILY_TRADES": 30,       // Max trades per day (1-150)
    "MAX_POSITION_SIZE": 5,       // Max position size as % of capital (0.5-20)
    "MAX_DRAWDOWN": 20,           // Max drawdown as % of capital (0-100)
    "TRADE_EXECUTION": true,      // Enable auto trade execution
    "MIN_CONFIDENCE": 75          // Min signal confidence for execution (50-90)
}